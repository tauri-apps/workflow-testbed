name: release

on:
  push:
    branches:
      - dev

jobs:
  dump:
    runs-on: ubuntu-16.04
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: Dump job context
        env:
          JOB_CONTEXT: ${{ toJson(job) }}
        run: echo "$JOB_CONTEXT"
      - name: Dump steps context
        env:
          STEPS_CONTEXT: ${{ toJson(steps) }}
        run: echo "$STEPS_CONTEXT"
      - name: Dump runner context
        env:
          RUNNER_CONTEXT: ${{ toJson(runner) }}
        run: echo "$RUNNER_CONTEXT"
      - name: Dump strategy context
        env:
          STRATEGY_CONTEXT: ${{ toJson(strategy) }}
        run: echo "$STRATEGY_CONTEXT"
      - name: Dump matrix context
        env:
          MATRIX_CONTEXT: ${{ toJson(matrix) }}
        run: echo "$MATRIX_CONTEXT"

  release:
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.message, 'Publish')
    steps:
      - uses: actions/checkout@v2
      - name: git config
        run: |
          git config --global user.name "${{ github.event.pusher.name }}"
          git config --global user.email "${{ github.event.pusher.email }}"
      - run: git fetch origin master
      - run: git checkout master
      - run: git merge --squash ${{ github.sha }} -s recursive -X theirs --allow-unrelated-histories
      - run: git commit -m "${{ github.event.head_commit.message }}"
      - run: git push

  publish-npm:
    runs-on: ubuntu-latest
    needs: release
    steps:
      - uses: actions/checkout@v2
        with:
          ref: master
      - uses: actions/setup-node@v1
        with:
          node-version: 12
          registry-url: 'https://registry.npmjs.org'
      - name: get version
        working-directory: cli/tauri.js
        run: echo ::set-env name=PACKAGE_VERSION::$(node -p "require('./package.json').version")
      - name: npm pack
        working-directory: cli/tauri.js
        run: npm pack
      - name: Publish package
        working-directory: cli/tauri.js
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          echo "We will publish this directory."
          ls # npm publish
          echo $PACKAGE_VERSION
      - name: Create Release
        id: create_npm_release
        uses: actions/create-release@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
        with:
          tag_name: tauri.js-v${{ env.PACKAGE_VERSION }}
          release_name: "Release tauri.js v${{ env.PACKAGE_VERSION }}"
          body: |
            Changes in this Release
            - First Change
            - Second Change
          draft: false
          prerelease: false
      - name: Upload Release Asset
        id: upload-release-asset 
        uses: actions/upload-release-asset@v1.0.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_npm_release.outputs.upload_url }} 
          asset_path: ./cli/tauri.js/tauri-${{ env.PACKAGE_VERSION }}.tgz
          asset_name: tauri-${{ env.PACKAGE_VERSION }}.tgz

  publish-cargo:
    runs-on: ubuntu-latest
    needs: release
    steps:
      - uses: actions/checkout@v2
        with:
          ref: master
      - run: git status
      - name: Login to Crates.IO
        run: echo "cargo login" # cargo login ${{ secrets.crate_token }}
      - name: Publish CLI
        run: |
          cd cli/tauri-cli
          echo "We will publish this directory."
          ls # cargo publish
      - name: Publish TAURI
        run: |
          cd tauri
          echo "We will publish this directory."
          ls # TAURI_DIST_DIR=../../test/fixture/dist cargo publish
